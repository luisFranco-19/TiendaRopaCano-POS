<UserControl x:Class="TiendaRopaCano.Presentation.Views.Ventas.VentasView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:helpers="clr-namespace:TiendaRopaCano.Presentation.Helpers"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900"
             Background="Transparent">

    <UserControl.Resources>
        <helpers:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>

        <!-- Estilo DataGrid oscuro moderno -->
        <Style x:Key="DarkDataGrid" TargetType="DataGrid">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Foreground" Value="#111827"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="RowBackground" Value="#FFFFFF"/>
            <Setter Property="AlternatingRowBackground" Value="#F8F9FA"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#E5E7EB"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="RowHeight" Value="44"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="CanUserResizeRows" Value="False"/>
        </Style>

        <!-- Estilo encabezado de columna -->
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#F3F4F6"/>
            <Setter Property="Foreground" Value="#4B5563"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Padding" Value="16,12"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
        </Style>

        <!-- Estilo fila seleccionada -->
        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="Transparent"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#E5E7EB"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E9ECEF"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="#111827"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Estilo botón de acción -->
        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Height" Value="38"/>
            <Setter Property="Padding" Value="20,0"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                CornerRadius="8"
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Estilo DatePicker oscuro -->
        <Style x:Key="DarkDatePicker" TargetType="DatePicker">
            <Setter Property="Foreground" Value="#111827"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Width" Value="140"/>
            <Setter Property="Height" Value="38"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DatePicker">
                        <Border x:Name="Root"
                                Background="#E5E7EB"
                                BorderBrush="#E2E8F0"
                                BorderThickness="1"
                                CornerRadius="8">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <DatePickerTextBox Grid.Column="0"
                                                  x:Name="PART_TextBox"
                                                  Foreground="#111827"
                                                  Background="Transparent"
                                                  BorderThickness="0"
                                                  Padding="10,0,4,0"
                                                  VerticalAlignment="Center"
                                                  FontFamily="Segoe UI"
                                                  FontSize="12"
                                                  CaretBrush="#0B2447"
                                                  SelectionBrush="#0B2447"/>
                                <Button Grid.Column="1"
                                        x:Name="PART_Button"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        Cursor="Hand"
                                        Width="32"
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Center">
                                    <TextBlock Text="📅"
                                               FontSize="13"
                                               VerticalAlignment="Center"
                                               HorizontalAlignment="Center"
                                               Foreground="#0B2447"/>
                                </Button>
                                <Popup x:Name="PART_Popup"
                                       PlacementTarget="{Binding ElementName=Root}"
                                       Placement="Bottom"
                                       AllowsTransparency="True"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="BorderBrush" Value="#0B2447"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Cabecera -->
            <RowDefinition Height="Auto"/> <!-- Barra de herramientas -->
            <RowDefinition Height="Auto"/> <!-- Loading -->
            <RowDefinition Height="*"/>    <!-- Tabla de Ventas -->
        </Grid.RowDefinitions>

        <!-- Cabecera de Historial de Ventas -->
        <Border Grid.Row="0" Margin="0,0,0,20" Padding="20,15" Background="#FFFFFF" CornerRadius="12" BorderBrush="#E5E7EB" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Width="46" Height="46" CornerRadius="12" Margin="0,0,15,0">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="#0B2447" Offset="0"/>
                                <GradientStop Color="#1E3A8A" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <Path Data="{StaticResource IconVentas}" Fill="White" Width="22" Height="22" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel>
                        <TextBlock Text="HISTORIAL DE VENTAS" Foreground="#1F2937" FontSize="18" FontWeight="Bold" FontFamily="Segoe UI"/>
                        <TextBlock Text="Consulta los registros históricos de transacciones realizadas, filtra por fechas y genera reportes" Foreground="#4B5563" FontSize="11" FontFamily="Segoe UI"/>
                    </StackPanel>
                </StackPanel>

                <Border Grid.Column="1" Background="#C89F70" CornerRadius="8" Padding="15,8" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Ventas Registradas: " Foreground="#FFFFFF" FontSize="13" VerticalAlignment="Center" Margin="0,0,5,0" FontFamily="Segoe UI" FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding Ventas.Count}" Foreground="#FFFFFF" FontWeight="Bold" FontSize="16" VerticalAlignment="Center" FontFamily="Segoe UI"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <!-- Barra de herramientas -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Filtros por fecha y Búsqueda -->
            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="Desde:"
                           Foreground="#4B5563"
                           FontSize="12"
                           FontFamily="Segoe UI"
                           FontWeight="SemiBold"
                           VerticalAlignment="Center"
                           Margin="0,0,6,0"/>
                <DatePicker SelectedDate="{Binding FechaDesde}"
                            Style="{StaticResource DarkDatePicker}"
                            Margin="0,0,12,0"/>

                <TextBlock Text="Hasta:"
                           Foreground="#4B5563"
                           FontSize="12"
                           FontFamily="Segoe UI"
                           FontWeight="SemiBold"
                           VerticalAlignment="Center"
                           Margin="0,0,6,0"/>
                <DatePicker SelectedDate="{Binding FechaHasta}"
                            Style="{StaticResource DarkDatePicker}"
                            Margin="0,0,12,0"/>

                <Button Content="🔍 Filtrar"
                        Command="{Binding FiltrarPorFechaCommand}"
                        Style="{StaticResource ActionButton}"
                        Background="#0F5132"
                        Margin="0,0,6,0"/>

                <Button Content="↻ Todas"
                        Command="{Binding CargarVentasCommand}"
                        Style="{StaticResource ActionButton}"
                        Background="#E2E8F0"
                        Foreground="#1F2937"
                        Margin="0,0,12,0"/>

                <Border Background="#E5E7EB" CornerRadius="8" Width="200" Height="38" Margin="0,0,8,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="30"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="🔍" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="#4B5563"/>
                        <TextBox Grid.Column="1" 
                                 Text="{Binding FiltroTexto, UpdateSourceTrigger=PropertyChanged}" 
                                 Background="Transparent" 
                                 BorderThickness="0" 
                                 Foreground="#1F2937" 
                                 VerticalContentAlignment="Center" 
                                 Margin="5,0,10,0"
                                 CaretBrush="#0B2447"
                                 FontSize="13"/>
                    </Grid>
                </Border>
            </StackPanel>

            <!-- Botón Nueva Venta -->
            <Button Grid.Column="1"
                    Content="＋ Nueva Venta"
                    Command="{Binding NuevaVentaCommand}"
                    Style="{StaticResource ActionButton}">
                <Button.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop Color="#0B2447" Offset="0"/>
                        <GradientStop Color="#1E3A8A" Offset="1"/>
                    </LinearGradientBrush>
                </Button.Background>
            </Button>
        </Grid>

        <!-- Loading -->
        <ProgressBar Grid.Row="2"
                     IsIndeterminate="{Binding IsLoading}"
                     Height="3"
                     Background="Transparent"
                     Foreground="#0B2447"
                     BorderThickness="0"
                     Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"
                     Margin="0,0,0,8"/>

        <!-- Tabla y Detalle de Ventas -->
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="360"/>
            </Grid.ColumnDefinitions>

            <!-- Tabla de Ventas (Izquierda) -->
            <Border Grid.Column="0"
                    CornerRadius="12"
                    Background="#FFFFFF"
                    BorderBrush="#E5E7EB"
                    BorderThickness="1"
                    Padding="10"
                    Margin="0,0,15,0">
                <DataGrid Style="{StaticResource DarkDataGrid}"
                          ItemsSource="{Binding Ventas}"
                          SelectedItem="{Binding VentaSeleccionada}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Fecha"
                                            Binding="{Binding Fecha, StringFormat={}{0:dd/MM/yyyy HH:mm}}"
                                            Width="170">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="Margin" Value="5,0"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="Vendedor / Usuario"
                                            Binding="{Binding Usuario.NombreCompleto}"
                                            Width="*">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="Margin" Value="5,0"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="Total Venta"
                                            Binding="{Binding Total, StringFormat=C2}"
                                            Width="120">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                    <Setter Property="Foreground" Value="#1F2937"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="HorizontalAlignment" Value="Right"/>
                                    <Setter Property="Margin" Value="0,0,10,0"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="Items"
                                            Binding="{Binding Detalles.Count}"
                                            Width="70">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <!-- Panel de Detalle de Factura (Derecha) -->
            <Border Grid.Column="1"
                    CornerRadius="12"
                    Background="#FFFFFF"
                    BorderBrush="#E5E7EB"
                    BorderThickness="1"
                    Padding="15">
                <Grid>
                    <!-- Placeholder cuando no hay selección -->
                    <TextBlock Text="📋 Selecciona una factura para ver su detalle"
                               Foreground="#4B5563"
                               FontSize="13"
                               FontStyle="Italic"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               TextAlignment="Center"
                               TextWrapping="Wrap">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding VentaSeleccionada}" Value="{x:Null}">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                    <!-- Detalle de la factura -->
                    <Grid>
                        <Grid.Style>
                            <Style TargetType="Grid">
                                <Setter Property="Visibility" Value="Visible"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding VentaSeleccionada}" Value="{x:Null}">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Grid.Style>

                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/> <!-- Cabecera de detalle -->
                            <RowDefinition Height="*"/>    <!-- Lista de artículos -->
                            <RowDefinition Height="Auto"/> <!-- Total y acción -->
                        </Grid.RowDefinitions>

                        <!-- Cabecera de detalle -->
                        <StackPanel Grid.Row="0" Margin="0,0,0,15">
                            <TextBlock Text="DETALLE DE FACTURA" Foreground="#1F2937" FontSize="14" FontWeight="Bold" FontFamily="Segoe UI" Margin="0,0,0,10"/>
                            <Border Background="#F4F6F8" CornerRadius="8" Padding="12" BorderThickness="1" BorderBrush="#E5E7EB">
                                <StackPanel>
                                    <TextBlock Text="{Binding VentaSeleccionada.VentaId, StringFormat='Factura: #{0}'}" Foreground="#0B2447" FontWeight="Bold" FontSize="13" FontFamily="Segoe UI"/>
                                    <TextBlock Text="{Binding VentaSeleccionada.Fecha, StringFormat='Fecha: {0:dd/MM/yyyy HH:mm}'}" Foreground="#1F2937" FontSize="11" FontFamily="Segoe UI" Margin="0,4,0,2"/>
                                    <TextBlock Text="{Binding VentaSeleccionada.Usuario.NombreCompleto, StringFormat='Vendedor: {0}'}" Foreground="#4B5563" FontSize="11" FontFamily="Segoe UI"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>

                        <!-- Lista de artículos -->
                        <DataGrid Grid.Row="1"
                                  Style="{StaticResource DarkDataGrid}"
                                  ItemsSource="{Binding VentaSeleccionada.Detalles}"
                                  HeadersVisibility="Column"
                                  BorderThickness="0"
                                  RowHeight="36"
                                  Background="Transparent">
                            <DataGrid.Resources>
                                <Style TargetType="DataGridCell">
                                    <Setter Property="BorderThickness" Value="0"/>
                                    <Setter Property="Padding" Value="4,4"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="DataGridCell">
                                                <Border Background="{TemplateBinding Background}"
                                                        Padding="{TemplateBinding Padding}">
                                                    <ContentPresenter VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Background" Value="Transparent"/>
                                            <Setter Property="Foreground" Value="#111827"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="Background" Value="#F3F4F6"/>
                                    <Setter Property="Foreground" Value="#4B5563"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                    <Setter Property="FontSize" Value="11"/>
                                    <Setter Property="Padding" Value="4,6"/>
                                    <Setter Property="BorderBrush" Value="#E5E7EB"/>
                                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                </Style>
                            </DataGrid.Resources>
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Prenda" Binding="{Binding Producto.Nombre}" Width="*"/>
                                <DataGridTextColumn Header="Cant" Binding="{Binding Cantidad}" Width="45">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="HorizontalAlignment" Value="Center"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="Subtotal" Binding="{Binding Subtotal, StringFormat=C2}" Width="95">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="HorizontalAlignment" Value="Right"/>
                                            <Setter Property="Foreground" Value="#1F2937"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                            </DataGrid.Columns>
                        </DataGrid>

                        <!-- Total y Acción -->
                        <StackPanel Grid.Row="2" Margin="0,15,0,0">
                            <Border Background="#F4F6F8" CornerRadius="8" Padding="15,12" Margin="0,0,0,10" BorderThickness="1" BorderBrush="#E5E7EB">
                                <Grid>
                                    <TextBlock Text="TOTAL COMPRA" Foreground="#4B5563" FontSize="11" FontWeight="SemiBold" VerticalAlignment="Center" FontFamily="Segoe UI"/>
                                    <TextBlock Text="{Binding VentaSeleccionada.Total, StringFormat=C2}" Foreground="#15803D" FontSize="18" FontWeight="Bold" HorizontalAlignment="Right" VerticalAlignment="Center" FontFamily="Segoe UI"/>
                                </Grid>
                            </Border>

                            <Button Command="{Binding GenerarFacturaPdfCommand}" Style="{StaticResource ActionButton}" Background="#0B2447" Height="40">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="📄 " VerticalAlignment="Center"/>
                                    <TextBlock Text="Descargar Factura PDF" VerticalAlignment="Center" FontWeight="Bold"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </Grid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
